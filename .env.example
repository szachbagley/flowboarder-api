PORT=3000
NODE_ENV=development

# Database Configuration
# Use 'localhost' if running API locally (npm run dev)
# Use 'mysql' if running API in Docker (docker-compose up)
DB_HOST=localhost
DB_USER=flowboarder
DB_PASSWORD=password
DB_NAME=flowboarder_db
DB_PORT=3306

# MySQL Root Password (for Docker container)
DB_ROOT_PASSWORD=rootpassword
